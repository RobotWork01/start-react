{
//+ "homepage": "https://<Githubアカウント名>.github.io/<リポジトリ名>/",
  "homepage": "https://robotwork01.github.io/start-react/",  
  "name": "start-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "start-react start",
    "build": "start-react build",
    "rm": "rm -rf docs",
    "mv": "mv build docs",
    "git": "git add . && git commit && git push origin master",
    "deploy": "npm run rm && npm run build && npm run mv && npm run git"
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    
    // 追加
    //"predeploy": "npm run build",
    //"deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "start-react",
      "start-react/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
